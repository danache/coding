使用两个指针分别从左边和右边向中间靠拢，每次比较当前存水量和最大存水量并更新，同时短板的那个向中间靠拢。
正确性证明：
反证法，假设当相遇时，我们获得的值不是最大，有一个max值比我们获得的还大。
那么设这个max值得左右边界al,ar。
停止时左右指针至只经过了al或者ar，我们假设为al。
使得左指针停在al处的情况有两种：
1另一个指针也指向了al，此时循环终止。但是这种情况违背了我们的假定，即两个指针没有同时指向最大值。
2.另一个指针到达了arr,此时的arr与al组成的水箱值大于al和ar.这个违背了我们的假定。因此，算法是正确的。
本问题关键在于水箱存的水在于短板。